#include <bits/stdc++.h>
using namespace std;

void test_case() {
    int f1, s1, f2, s2;
    cin >> f1 >> s1 >> f2 >> s2;
    int ans = 4;
    if (f1 - f2 < 0 or s1 - s2 < 0 ) {
        ans--;
    } 
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        test_case();
    }
}