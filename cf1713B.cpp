#include <bits/stdc++.h>
using namespace std;

void test_case() {
    int n;
    vector<int> arr;
    cin >> n;
    arr.resize(n);
    for (auto &a : arr) {
        cin >> a;
    }
    bool upward = true, downward = true;
    int ans = 0;
    for(int i=1; i<n; i++){
        if(arr[i]>=arr[i-1]){
            upwar
        }
    }

}

int main() {
    int t;
    cin >> t;
    while (t--) {
        test_case();
    }
}